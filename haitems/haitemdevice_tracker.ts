import { State } from '../hamain/state'
import { HaParentItem } from './haparentitem';

export default class HaItemDeviceTracker extends HaParentItem {
    public constructor(item: State) {
        super(item);
    }
}
